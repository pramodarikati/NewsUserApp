<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.NewsUserApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Day Mode Colors -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:textColorPrimary">@android:color/black</item> <!-- Day text color -->
        <item name="android:textColorHint">@android:color/darker_gray</item> <!-- Day hint color -->
        <item name="android:colorBackground">@android:color/white</item> <!-- Day background -->

    </style>
</resources>
