<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.NewsUserApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:textColorPrimary">@android:color/white</item> <!-- Night text color -->
        <item name="android:textColorHint">@android:color/white</item> <!-- Night hint color -->
        <item name="android:colorBackground">@android:color/black</item> <!-- Night background -->
    </style>
</resources>
